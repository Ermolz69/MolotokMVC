@model MolotokMvc.Models.Resume

<div class="container">
    <div class="card">
        <div class="card-header">
            <h3>Resume: @Model.Position</h3>
        </div>
        <div class="card-body">
            <p><strong>Description:</strong> @Model.Description</p>
            <p><strong>Tags:</strong> @Model.Tags</p>
            <p><strong>Created At:</strong> @Model.CreatedAt.ToShortDateString()</p>
        </div>
        <div class="card-footer">
            <button onclick="window.history.back()" class="btn bg-primary text-white">Back</button>
        </div>
    </div>
</div>
